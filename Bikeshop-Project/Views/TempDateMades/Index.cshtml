@model IEnumerable<Bikeshop_Project.Models.TempDateMade>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";

    // if a user is logged in, update logging information for monitoring team metrics
    if (Logger.returnUserLogInStatus())
    {
        Logger.numberofPageViews++;         // Add number of page views for general user session
        Logger.LogPageClick("Temp Dates Made");    // send log to monitoring team to show a new page click has occured.    
    }
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MADECOUNT)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MADECOUNT)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DATEVALUE">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DATEVALUE">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DATEVALUE">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
